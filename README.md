# business-os
BusinessOS is an offline first business operations suite built as a single HTML file. It includes a client CRM, invoice generator, expense splitter, pricing calculator, and contract checklist, all running fully in the browser using localStorage with no backend or accounts.
